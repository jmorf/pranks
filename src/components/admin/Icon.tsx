'use client'

import React from 'react'

export function Icon() {
  return (
    <div style={{
      width: '25px',
      height: '25px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontWeight: 900,
      fontSize: '16px',
      color: '#000',
    }}>
      P
    </div>
  )
}
